replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent
  tag: '1.21'

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false

resources:
  limits:
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
